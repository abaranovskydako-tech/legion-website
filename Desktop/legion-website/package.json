{
  "name": "legion-website",
  "version": "1.0.0",
  "description": "Фронтенд для аутсорсинговой компании Legion",
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview --port 3000",
    "deploy": "npm run build && git add . && git commit -m 'build: production' && git push origin main"
  },
  "devDependencies": {
    "vite": "^7.1.3"
  },
  "keywords": ["legion", "аутсорсинг", "персонал", "vite"],
  "author": "Legion Team",
  "license": "MIT"
}
